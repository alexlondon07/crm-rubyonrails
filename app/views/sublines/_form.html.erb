<%= form_for(@subline) do |f| %>
  <% if @subline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subline.errors.count, "error") %> prohibited this subline from being saved:</h2>

      <ul>
      <% @subline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
